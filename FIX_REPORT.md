# BTC交易面板闪退修复报告

## 问题诊断
闪退的根本原因是语法错误：
- **文件**: `btc_panel_qt.py` 第615行
- **错误**: `try-except` 块缩进错误
- **具体问题**: `if self.sig_label.text() in ["- 等待数据...", ""]:` 这行代码错误地放在了 `try` 块内，应该在 `except` 之前

## 修复内容
1. **修复语法错误**: 将第615-617行的代码移入 `try` 块内
2. **验证修复**: 
   - 语法检查通过 ✅
   - 导入测试通过 ✅  
   - 窗口创建测试通过 ✅
3. **创建备份**: `backup_docs/btc_panel_qt_fixed_syntax.py`

## 如何启动面板

### 方法1: 使用安全启动脚本（推荐）
```bash
cd D:\BTC
python run_safe.py
```

### 方法2: 直接运行主面板
```bash
cd D:\BTC
python btc_panel_qt.py
```

### 方法3: 使用批处理文件
双击 `start_panel.bat`

## 启动检查清单
✅ 1. 工作目录正确: `D:\BTC`  
✅ 2. Python依赖完整: PySide6, MetaTrader5, numpy, matplotlib  
✅ 3. 核心模块导入正常: `btc_panel`  
✅ 4. GUI模块导入正常: `btc_panel_qt`  
✅ 5. 语法检查通过: 无语法错误  

## 如果再次闪退
请按以下步骤排查：

1. **查看错误信息**:
   ```bash
   cd D:\BTC
   python run_safe.py
   ```
   
2. **检查运行日志**:
   - 面板底部的日志窗口会显示实时状态
   - 如果有错误会显示红色警告

3. **常见问题**:
   - MetaTrader5未安装或路径不正确
   - Python依赖包缺失
   - 文件权限问题

## 技术细节
- **修复时间**: 2026-06-26 00:05
- **Git提交**: `8cb4cd0` - "修复try-except语法错误，面板应该可以正常启动了"
- **测试状态**: 所有测试通过
- **文件状态**: 语法验证通过，导入测试通过，窗口创建测试通过

## 预期结果
修复后，面板应该能够正常启动，显示以下界面：
1. 价格显示区
2. 信号卡（主信号+实时信号合并）
3. 交易控制按钮
4. 日志窗口
5. 参数配置区

---

**高级诊断**: 如果面板启动后立即闪退，可能是：
1. MetaTrader5连接失败 - 检查MT5是否已安装
2. 显卡/显示驱动问题 - 尝试禁用硬件加速
3. 内存不足 - 关闭其他应用程序

**紧急恢复**: 如果需要回退到修复前的版本：
```bash
cd D:\BTC
git checkout HEAD~1
```

**技术支持**: 请提供 `python run_safe.py` 的完整输出，包含任何错误信息。